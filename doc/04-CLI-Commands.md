# CLI Commands

This describes the available CLI commands for this module.

## Cache

The module uses the Icinga Web FileCache to store data fetched from the backends.

Available commands:

```bash
# Clears the entire FileCache used by the Perfdatagraphs module
icingacli perfdatagraphs cache clear
```
